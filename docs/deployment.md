# ⚙️ Інструкція з розгортання — TaxiApp

Цей документ описує, як локально розгорнути проєкт **TaxiApp** (Node.js + Express + PostgreSQL).

---

## 1️. Вимоги (Prerequisites)

Перед початком встанови:

- **Git**
- **Node.js (LTS)** + **npm**
- **PostgreSQL** (разом з утилітою `psql` або PgAdmin)

Перевірка в терміналі:

```bash
node -v
npm -v
psql --version
```
## 2. Клонування репозиторію
```bash
git clone https://github.com/romansxxq/Coursework_TaxiApp.git
cd Coursework_TaxiApp
```
## 3️. Встановлення залежностей
```bash
npm install
```
- Папка node_modules/ генерується автоматично і не повинна комітитися в GitHub.

## 4️. Налаштування змінних середовища (.env)
У корені проєкту є файл-шаблон .env.example.
Створи реальний файл .env:

```bash
cp .env.example .env
# або створи вручну файл .env
```
# Приклад вмісту .env:

```env
PORT=3000

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=taxiapp

SESSION_SECRET=change_me
```
**Файл .env НЕ комітиться в GitHub.
У репозиторії залишається тільки .env.example.**

## 5️. Розгортання бази даних PostgreSQL
### 5.1 Створення бази даних
**Зайди в psql або інший клієнт і виконай:**

```bash
CREATE DATABASE taxiapp;
```
**За потреби створення окремого користувача:**
```bash
CREATE USER taxiapp_user WITH ENCRYPTED PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE taxiapp TO taxiapp_user;
```
**Тоді в .env:**
```bash
DB_USER=taxiapp_user
DB_PASSWORD=your_password
DB_NAME=taxiapp
```
### 5.2 Імпорт структури (DDL) з db.sql

**У корені проєкту є файл db.sql. Запусти:**

```bash
psql -U postgres -d taxiapp -f db.sql
```
**Або, якщо використовуєш користувача taxiapp_user:**
```bash
psql -U taxiapp_user -d taxiapp -f db.sql
```
## 6️. Запуск застосунку
**Варіант A (через npm-скрипт, якщо є)**
```bash
npm start
```
**Варіант B (напряму через Node.js)**
```bash
node server.js
```
## 7️. Перевірка роботи
- Відкрий у браузері: **[http://localhost:3000](http://localhost:3000)**
- Якщо ти змінив порт у .env (PORT=...), тоді використовуй свій порт.

## 8️. Типові проблеми та рішення
**🔴 Не підключається до бази даних**
Перевір у файлі .env:
```bash
DB_HOST, DB_PORT

DB_USER, DB_PASSWORD

DB_NAME
```
- Переконайся, що служба PostgreSQL запущена.

**🔴 Команда psql не знайдена**
- На Windows: перевір, чи встановлено PostgreSQL і чи додано його в PATH, або використовуй SQL Shell (psql)
- Альтернатива: імпорт через PgAdmin → Query Tool → відкрити db.sql та виконати.

**🔴 Немає прав доступу**
Переконайся, що користувач БД має права на базу:

```bash
GRANT ALL PRIVILEGES ON DATABASE taxiapp TO taxiapp_user;
Після цього повтори імпорт db.sql.
```
```bash
yaml
```
---
